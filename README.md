# qgis-interpolate-points-on-lines
QGIS Processing script that builds ZM line geometries, evenly spaced points, and line segments from a line layer and measurement points along it. Interpolates Z-values between points and encodes station (distance along the line) as M — useful for pipeline, road, or river profile data.
